/** @format */

const initialState = {};

const tempreducer = (state = initialState, action) => {
	switch (action.type) {
		case "1":
			return {};
		default:
			return state;
	}
};

export default tempreducer;
